import Vue from 'vue'

export const DispatchLocations = Vue.component('dispatchLocations', require('./DispatchLocations.vue').default)

export const DispatchLocation = Vue.component('dispatchLocation', require('./DispatchLocation.vue').default)
