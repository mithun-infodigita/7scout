export const groups = (state) => {
    return state.groups
}

export const group = (state) => {
    return state.group
}

export const selectedTab = (state) => {
    return state.selectedTab
}

export const groupColumn = (state) => {
    return state.groupColumn
}

export const groupColumns = (state) => {
    return state.groupColumns
}
