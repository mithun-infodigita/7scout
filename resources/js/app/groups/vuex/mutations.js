export const SET_GROUPS_DATA = (state, data) => {
    state.groups = data
}

export const SET_GROUP_DATA = (state, data) => {
    state.group = data
}

export const SET_SELECTED_TAB = (state, value) => {
    state.selectedTab = value
}

export const SET_GROUP_COLUMNS_DATA = (state, data) => {
    state.groupColumns = data
}

export const SET_GROUP_COLUMN_DATA = (state, data) => {
    state.groupColumn = data
}
