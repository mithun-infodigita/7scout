import { PartIndexes } from '../views'

export default [
    {
        path: '/partIndexes',
        component: PartIndexes,
        name: 'partIndexes',
        meta: {
            guest: false,
            needsAuth: true
        }
    }
]
