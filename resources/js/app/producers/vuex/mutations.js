export const SET_PRODUCERS_DATA = (state, data) => {
    state.producers = data
}

export const SET_PRODUCER_DATA = (state, data) => {
    state.producer = data
}

export const SET_SELECTED_TAB = (state, value) => {
    state.selectedTab = value
}
