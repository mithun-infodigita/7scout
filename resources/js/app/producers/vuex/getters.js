export const producers = (state) => {
    return state.producers
}

export const producer = (state) => {
    return state.producer
}

export const selectedTab = (state) => {
    return state.selectedTab
}
