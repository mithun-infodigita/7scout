export const SET_USERS_DATA = (state, data) => {
    state.users = data
}

export const SET_USER_DATA = (state, data) => {
    state.user = data
}

export const SET_SELECTED_TAB = (state, value) => {
    state.selectedTab = value
}

export const SET_TOKEN_DATA = (state, data) => {
    state.token = data
}
